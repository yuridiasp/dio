export interface ICard {
    userPicture: string,
    imageBackground: string,
    author: string,
    date: string,
    postTitle: string,
    postText: string,
    postTags: string,
    likes: number
}